/**
 * vv-switch 测试入口（仅用于手动运行单个文件）
 *
 * 默认测试通过 `npm test` 运行，自动发现 test/**/*.test.ts：
 *   npm test                跑全部单元/组件测试
 *   npm run test:integration 跑集成测试（需真实上游 API）
 *
 * 手动运行单个文件：
 *   npx tsx --test test/sse-pipe.test.ts
 */

export {};
